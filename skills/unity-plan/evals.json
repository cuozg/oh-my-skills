[
  {
    "query": "How long will it take to fix the typo in HealthComponent.cs?",
    "should_trigger": true,
    "assertions": [
      "should detect scope as XS with High confidence",
      "should present SCOPE DETECTION template with reasoning and signals",
      "should BLOCK and wait for user confirmation before generating plan",
      "should route to Quick mode after confirmation"
    ]
  },
  {
    "query": "Plan adding a pause menu with resume and quit buttons",
    "should_trigger": true,
    "assertions": [
      "should detect scope as S with High confidence",
      "should present scope reasoning citing single-system UI work",
      "should BLOCK for scope confirmation",
      "should generate inline Quick report after user confirms",
      "should present follow-up options and BLOCK again before creating tasks"
    ]
  },
  {
    "query": "Plan the inventory system feature — it spans UI, data persistence, and networking",
    "should_trigger": true,
    "assertions": [
      "should detect scope as M or L with Medium confidence",
      "should cite multi-system involvement (UI, data, networking) in reasoning",
      "should BLOCK for scope confirmation",
      "should generate Deep mode markdown plan after confirmation",
      "should write plan to Documents/Plans/PLAN_Inventory.md or similar",
      "should BLOCK again and ask before creating tasks"
    ]
  },
  {
    "query": "Plan refactoring the entire input system to use the new Input System package across all player controllers, UI, and camera systems",
    "should_trigger": true,
    "assertions": [
      "should detect scope as L with Medium confidence",
      "should cite cross-system refactor signals in reasoning",
      "should BLOCK for scope confirmation",
      "should route to Deep mode with full investigation",
      "should produce task hierarchy with blockedBy dependencies"
    ]
  },
  {
    "query": "Actually this is bigger than I thought, make it an L",
    "should_trigger": true,
    "assertions": [
      "should accept user scope adjustment to L",
      "should re-present updated scope as L",
      "should switch to Deep mode planning"
    ]
  },
  {
    "query": "Write a MonoBehaviour script that handles player movement with WASD keys",
    "should_trigger": false
  },
  {
    "query": "Fix the NullReferenceException in PlayerController.cs line 42",
    "should_trigger": false
  }
]
